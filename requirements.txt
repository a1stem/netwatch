# NetWatch requirements
# Install with: pip3 install -r requirements.txt
#
# PyQt5 is available via apt (preferred on Ubuntu/Debian):
#   sudo apt install python3-pyqt5
# or via pip:
#   pip3 install PyQt5

psutil>=5.9.0
PyQt5>=5.15.0

# Optional — offline GeoIP country lookup (free, no API key needed).
# Download GeoLite2-Country.mmdb from:
#   https://dev.maxmind.com/geoip/geolite2-free-geolocation-data
# Place it at:  netwatch/data/GeoLite2-Country.mmdb
maxminddb>=1.5.0
